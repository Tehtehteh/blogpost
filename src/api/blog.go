package api

import (
	"github.com/valyala/fasthttp"
)

func BlogPostHandler(ctx *fasthttp.RequestCtx){
	//var blog_post_id int
	//channel := make(chan db.BlogPost)
	//blog_post_id = ctx.QueryArgs().GetUintOrZero("id")
	//if blog_post_id != 0 {
	//	db.GetBlogPost(blog_post_id, channel)
	//}
}